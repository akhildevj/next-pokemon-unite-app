import UniteContainer from './UniteContainer';
import UniteFilter from './UniteFilter';

const UniteHome = () => {
  return (
    <main className='main'>
      <UniteFilter />
      <UniteContainer />
    </main>
  );
};

export default UniteHome;
